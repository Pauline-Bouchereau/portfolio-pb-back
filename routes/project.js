// Import packages
const express = require("express");
const router = express.Router();

// Route to create a project in BDD

// Rooute to delete a project from BDD

// Route to gat all projects (with filters)

// Export the routes
module.exports = router;
